extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 That one time JavaScript blew your mind

      p.lead.text-light
        | Well, actually this was just made for a project to get a feeling for working with Node.js, Express and Pug.
        | If you read this that means I was successful, at least to some extent.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        a(href=`/project/${project.id}`)
          img.thumbnail(src=`${project.image_urls[0]}`)
          h5= project.project_name
      .cell
      
      
